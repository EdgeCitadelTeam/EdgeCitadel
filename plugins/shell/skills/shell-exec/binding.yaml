apiVersion: edgecitadel.io/v1alpha1
kind: AgentSkillBinding

skillId: edgecitadel.shell.exec
version: 0.1.0

execution:
  kind: runtime-handler
  name: shell.exec

schemas:
  input: schemas/input.json
  output: schemas/output.json

requires:
  knowledge: []
  network: []
  devices: [host-shell]

extensions: {}
