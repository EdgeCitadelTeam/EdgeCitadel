{
  "name": "edgecitadel-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for OpenClaw Swarm Control Dashboard",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "axios": "^1.7.9",
    "mqtt": "^5.0.0",
    "ws": "^8.18.0"
  }
}
